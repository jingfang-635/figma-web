---
description: Figma 全栈生成默认启用 antd + Layout IR + Screen Blueprint + 双闸门（字段 + SSIM）
alwaysApply: true
---

# 默认前端还原方案

本仓库从 Figma 生成全栈时**默认**采用高还原方案，无需用户额外指定。

## 流水线

init-project → App Spec（人工闸门）→ **Layout IR → Visual IR → Screen Blueprint** → codegen → **双闸门（字段一致性 + SSIM 视觉闸门）** → 还原轮次

## 必须遵守

- 前端：**antd + @ant-design/icons + recharts + dayjs**，`ConfigProvider` 主题由脚本生成
- **禁止**通用 `ResourcePage` 或自制 Button/Table/Modal 覆盖全部业务屏
- 项目差异全部由 `fixtures/<slug>/app-spec.json` 驱动（标杆屏 `benchmarkScreens` / 路由 / 闸门账号 / 品牌）；**脚本与流程禁止业务硬编码**
- 必跑：`npm run visual:all`（layout / extract / shots / gen / assets / fields / gate）
- 标杆屏由 `benchmarkScreens` 声明（覆盖图表/列表/表单/详情/弹窗 5 种模式）；SSIM ≥ 0.97 或 mismatch < 2%
- 字段一致性：`screenConfigs.ts` 中 `needsReview: true` 数量 = 0，且逐屏字段与 `fixtures/figma-fields.json` 逐字一致
- **第一版全栈之后**必须进入字段还原 + 页面还原轮次：Playwright 逐页截图对比，列出差异并修代码；每轮结束询问是否下一轮，直至用户选择不进入
- Blueprint：`fixtures/<slug>/screen-blueprints/` → `apps/web/src/blueprints/`
- 图标必须来自 Layout IR 导出的 `public/assets`，禁止 emoji 冒充

## 文档

- 仓库：`figma-to-fullstack/visual-fidelity.md`（完整方案）
- 仓库：`figma-to-fullstack/SKILL.md`（流水线）
- 用户级：`~/.cursor/skills/figma-to-fullstack/`
- 智能体：`figma-to-fullstack/figma-to-fullstack.md`

## 验收

未过双闸门（`visual:fields` + `visual:gate`）不得宣称生成完成。第一版全栈后须跑还原轮次（见 visual-fidelity.md「还原轮次」），用户未明确停止前不得收尾。