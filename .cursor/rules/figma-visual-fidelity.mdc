---
description: Figma 全栈生成启用 Layout IR + Screen Blueprint + 双闸门（字段 + SSIM）；技术栈由用户逐层选择，无默认
alwaysApply: true
---

# 前端还原方案（按用户选择的栈适配）

本仓库从 Figma 生成全栈时采用高还原方案（Layout IR → Visual IR → Blueprint → 双闸门 → 还原轮次）。

## 技术栈：完全由用户逐层选择，无默认

- **不存在预设默认栈**；后端语言 → 前端框架 → 后端框架 → 数据库 → ORM，**各层单独一问**，缺一项就停，不猜
- 选项随语言联动过滤（如选 Java 时 ORM 只列 JPA / MyBatis，不得出现 Prisma）
- 前端 UI 组件库随所选框架适配（React 生态可选 antd / MUI / Mantine 等；Vue 生态可选 Element Plus / Ant Design Vue 等），**不以任何框架/组件库作为硬性条款**
- 脚本生成的主题资产（tokens / theme / Blueprint）必须按用户选定的框架与组件库适配输出

## 流水线

init-project → App Spec（人工闸门）→ **Layout IR → Visual IR → Screen Blueprint** → codegen → **双闸门（字段一致性 + SSIM 视觉闸门）** → 还原轮次

## 必须遵守

- **禁止**通用 `ResourcePage` 或自制 Button/Table/Modal 覆盖全部业务屏（组件一律取自用户选定的组件库）
- 项目差异全部由 `fixtures/<slug>/app-spec.json` 驱动（screens 全屏清单 / 路由 / 闸门账号 / 品牌）；**脚本与流程禁止业务硬编码**
- 必跑：`npm run visual:all`（layout / extract / shots / gen / assets / fields / gate）
- 全屏闸门：`screens` 全量屏（含弹窗）一律进 SSIM 闸门，无标杆/非标杆之分；SSIM ≥ 0.97 或 mismatch < 2%
- 字段一致性：`screenConfigs` 中 `needsReview: true` 数量 = 0，且逐屏字段与 `fixtures/figma-fields.json` 逐字一致
- **第一版全栈之后**必须进入字段还原 + 页面还原轮次：Playwright 逐页截图对比，列出差异并修代码；每轮结束询问是否下一轮，直至用户选择不进入
- Blueprint：`fixtures/<slug>/screen-blueprints/` → `apps/web/src/blueprints/`
- 图标必须来自 Layout IR 导出的 `public/assets`，禁止 emoji 冒充

## 文档

- 仓库：`figma-to-fullstack/visual-fidelity.md`（完整方案）
- 仓库：`figma-to-fullstack/SKILL.md`（流水线）
- 用户级：`~/.cursor/skills/figma-to-fullstack/`
- 智能体：`figma-to-fullstack/figma-to-fullstack.md`

## 验收

未过双闸门（`visual:fields` + `visual:gate`）不得宣称生成完成。第一版全栈后须跑还原轮次（见 visual-fidelity.md「还原轮次」），用户未明确停止前不得收尾。